### LCA

#### 倍增法实现的LCA

用法注意:
1. `fa[0]`, `dep` 两个数组是必要的
2. 首先需要输入 `fa`, 可为每个点单独设置, 也可以传数组批量设置
3. 输入fa后需要调用 `finishInput` 完成一些初始化
4. `dep` 可手动输入; 如果复杂度允许, 本类提供了一个 $ O(NlogN) $ 的方法 `calDepth`. **但该方法调用前必须调用 `finishInput`.**
5. 若有实例 `lca`, 可通过 `lca(x, y)` 询问 x, y 的 lca.